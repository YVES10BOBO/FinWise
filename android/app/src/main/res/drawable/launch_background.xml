<?xml version="1.0" encoding="utf-8"?>
<!-- Launch splash background (pre-Android 21 fallback). Kept in sync with
     drawable-v21 so the branded colour is identical on every device. -->
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/finwise_splash" />
</layer-list>
